extends layout

include mixins/crearProyecto
block contenido
    .contenido-principal
        
        br
        if errores 
            each error in errores
                .alerta= error.texto

        +crearProyecto(proyecto)
            